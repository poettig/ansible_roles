---

- name: "Merge package list"
  ansible.builtin.set_fact:
    packages_merged_list: "{{ lookup('community.general.merge_variables', 'packages_', pattern_type='prefix') }}"

- name: "Install packages"
  ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] | lower }}.yml"
